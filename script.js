// function calculateBonus(a,b) {
//   let bonus;
//   const sum = a + b;
//   sum > 50 ? (bonus = 50) : (bonus = sum);
//   return bonus;
// };

function sum(a,b){
  return a+b;
}

module.exports = sum;
